_obj = argument0

/*var still_searching = true
while (still_searching)
{
    _obj = instance_nearest(_obj.x, _obj.y, resource)
    if _obj == noone
    {
        still_searching = false
    }
    else
    {
        _dx = _obj.x - _obj.x
        _dy = _obj.y - _obj.y
        if (_dx < 20) and (_dy < 20)
            instance_destroy(_obj)
        else
            still_searching = false
    }
}*/
